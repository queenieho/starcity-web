{:webserver {:port 8080}

 :datomic   {:schema-dir "datomic/schemas"
             :seed-dir   "datomic/seed"
             :partition  :db.part/user}

 :logger    {:level   :trace
             :logfile "logs/server.log"}

 :nrepl     {:port 7889}}
